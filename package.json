{
  "name": "compiler-companion",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev -w @compiler-companion/web\" \"npm run dev -w @compiler-companion/api\"",
    "build": "npm run build -w @compiler-companion/shared && npm run build -w @compiler-companion/api && npm run build -w @compiler-companion/web",
    "postinstall": "npm run build -w @compiler-companion/shared",
    "test": "npm run test -w @compiler-companion/api",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"**/*.{ts,tsx,js,json}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json}\"",
    "check": "npm run lint && npm run format:check"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/ws": "^8.18.1",
    "concurrently": "^9.1.2",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript-eslint": "^8.18.2"
  },
  "dependencies": {
    "msedge-tts": "^2.0.7",
    "ws": "^8.21.3"
  }
}
